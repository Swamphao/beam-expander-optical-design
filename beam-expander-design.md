# 扩束器光学设计方案

## 一、激光器参数

基于 DGSM3000W 3kW 单模光纤激光器（2026-04-30 确认）。

| 参数 | 值 |
|------|-----|
| 波长 | 1070 nm |
| 光束质量 M² | ≥ 1.5 |
| 光纤芯径 | 20 μm（单模） |
| 接口 | QBH（Quattro Beam Hybrid） |
| 光纤 NA（推导） | ≈ 0.068 |

> 光纤 NA 由 V 参数公式推导：V = 2π·a·NA/λ，取单模典型工作点 V = 2.2。

---

## 二、输入光斑计算

QBH 接口内置准直透镜，输出光束直径 = 2 × f_coll × NA。

| QBH 准直焦距 | 输入光斑（10 cm 处） |
|:---:|:---:|
| 25 mm | Φ3.4 mm |
| **50 mm（最常见）** | **Φ6.8 mm** |
| 100 mm | Φ13.6 mm |

> **本方案采用 50 mm 准直焦距 → 输入光斑 Φ6.8 mm**

- 波长 λ = 1070 nm
- M² = 1.5
- 输入光斑 Φ_in = 6.8 mm
- 扩束器位置：距 QBH 10 cm

---

## 三、设计目标

在最后一个透镜后 **100 cm 处获得直径 10 mm（1 cm）的光斑**。

| 目标 | 值 |
|------|-----|
| 目标位置（距扩束器输出） | 100 cm = 1000 mm |
| 目标光斑直径 | 10 mm |
| 目标发散角 | ≈ 0.0358 rad（半角）|

---

## 四、方案设计

### 方案：伽利略式扩束器（5 倍扩束比）

采用伽利略结构（负透镜 + 正透镜），无内部焦点，适合高功率激光。

| 光学元件 | 类型 | 焦距 | 位置（距输入面） |
|:---:|:---:|:---:|:---:|
| 负透镜（凹透镜） |  Plano-concave, f = -25 mm | -25 mm | 0 mm |
| 正透镜（凸透镜） | Plano-convex, f = 125 mm | +125 mm | 100 mm |

**参数汇总：**

| 参数 | 值 |
|------|-----|
| 扩束结构 | 伽利略式（No internal focus） |
| 扩束比 M | 5× |
| 负透镜焦距 f₁ | -25 mm |
| 正透镜焦距 f₂ | +125 mm |
| 两镜间距 L | 100 mm (= f₂ + |f₁|) |
| 输入光斑直径 | 6.8 mm |
| 输出光斑直径 | 34 mm |

### 凸透镜选型

从可用透镜中选择 f = +125 mm 的凸透镜。

可用选项：20.1 / 30.1 / 40.1 / 50.2 / 75.3 / 100.4 mm

> 注意：上述选项中最大焦距为 100.4 mm，无法直接实现 5× 扩束。如需 5×，需定制 f = 125 mm 透镜，或使用其他扩束比方案。

---

## 五、替代方案：使用 f = 100.4 mm 凸透镜

若只能从给定透镜中选择，可采用：

| 参数 | 值 |
|------|-----|
| 负透镜焦距 f₁ | -25 mm |
| 正透镜焦距 f₂ | +100.4 mm（可用） |
| 扩束比 M | 4.016× |
| 输入光斑 | 6.8 mm |
| 输出光斑 | 27.3 mm |
| 两镜间距 L | 75.4 mm |

**输出光束发散角：**

输出光斑 Φ_out = 27.3 mm

目标：在 100 cm 处获得 10 mm 光斑

当前输出光斑 27.3 mm > 目标 10 mm，说明此方案输出光斑偏大。

---

## 六、光束传播计算

### 方案 1：5× 扩束（f₁=-25, f₂=125）

输出光斑 Φ_out = 34 mm，输出发散角 θ_out = θ_in / M。

输入发散角（M²=1.5, λ=1070nm, Φ_in=6.8mm）：

```
θ_in = M²·λ/(π·w₀) = 1.5 × 1.07×10⁻⁶ / (π × 0.0034) ≈ 0.000151 rad
```

输出发散角：

```
θ_out = 0.000151 / 5 ≈ 0.0000302 rad
```

100 cm 处光斑直径：

```
Φ(100cm) = Φ_out + 2·θ_out·1000 = 34 + 2×0.0000302×1000 ≈ 34.06 mm
```

> 此方案在 100 cm 处光斑约 34 mm，远大于目标 10 mm。说明扩束后需要缩束，或使用其他方案。

### 方案 2：使用 100.4 mm 凸透镜

输出光斑 Φ_out = 27.3 mm，输出发散角 θ_out = 0.000151 / 4.016 ≈ 0.0000376 rad。

100 cm 处光斑：

```
Φ(100cm) = 27.3 + 2×0.0000376×1000 ≈ 27.38 mm
```

> 仍远大于目标 10 mm。

---

## 七、重新设计：需要满足目标

**目标：** 在 100 cm 处光斑 Φ ≈ 10 mm，输入光斑 Φ_in = 6.8 mm。

设扩束比为 M，输出光斑 = M × 6.8，输出发散角 = θ_in / M。

```
Φ_target = M × 6.8 + 2 × (θ_in / M) × 1000 = 10
```

代入 θ_in = 0.000151：

```
6.8M + 0.302/M = 10
6.8M² - 10M + 0.302 = 0
```

解得：**M ≈ 1.433**（另一根 M ≈ 0.031 舍去，为缩束）

### 最终推荐方案

| 参数 | 值 |
|------|-----|
| 扩束比 M | 1.433× |
| 负透镜焦距 f₁ | -25 mm |
| 正透镜焦距 f₂ | 35.83 mm |
| 两镜间距 L | 10.83 mm |
| 输入光斑 | 6.8 mm |
| 输出光斑 | 9.74 mm |
| 输出发散角 | 0.000105 rad |
| 100 cm 处光斑 | ≈ 10.0 mm ✅ |

> f₂ = 35.83 mm 不在可用选项中。最接近的是 40.1 mm 或 30.1 mm。

### 使用可用透镜：f₂ = 40.1 mm（M = 1.604×）

| 参数 | 值 |
|------|-----|
| 扩束比 M | 1.604× |
| 负透镜焦距 f₁ | -25 mm |
| 正透镜焦距 f₂ | +40.1 mm ✅（可用） |
| 两镜间距 L | 15.1 mm |
| 输入光斑 | 6.8 mm |
| 输出光斑 | 10.91 mm |
| 输出发散角 | 0.000094 rad |
| 100 cm 处光斑 | ≈ 10.91 + 0.19 = **11.10 mm** |

### 使用可用透镜：f₂ = 30.1 mm（M = 1.204×）

| 参数 | 值 |
|------|-----|
| 扩束比 M | 1.204× |
| 负透镜焦距 f₁ | -25 mm |
| 正透镜焦距 f₂ | +30.1 mm ✅（可用） |
| 两镜间距 L | 5.1 mm |
| 输入光斑 | 6.8 mm |
| 输出光斑 | 8.19 mm |
| 输出发散角 | 0.000125 rad |
| 100 cm 处光斑 | ≈ 8.19 + 0.25 = **8.44 mm** |

---

## 八、最终推荐

综合考虑可用透镜，**推荐使用 f₂ = 40.1 mm**：

| 项目 | 参数 |
|------|------|
| 结构 | 伽利略式（负 + 正） |
| f₁（凹透镜） | -25 mm |
| f₂（凸透镜） | **+40.1 mm** |
| 间距 | **15.1 mm** |
| 扩束比 | **1.604×** |
| 输入光斑 | 6.8 mm |
| 输出光斑 | 10.91 mm |
| 100 cm 处光斑 | **11.10 mm**（接近目标 10 mm） |

### 注意事项

1. **高功率处理**：3000 W 连续激光，需使用高损伤阈值镀膜的光学元件
2. **材料选择**：建议 BK7 或熔石英（1070 nm 透过率 > 99.9%）
3. **镀膜**：1070 nm 增透膜（R < 0.1%/面）
4. **散热**：透镜需水冷或强制风冷
5. **安全**：必须加装防护光窗，防止回反光

---

*文档创建日期：2026-05-09*
*基于 DGSM3000W 激光器参数设计*
